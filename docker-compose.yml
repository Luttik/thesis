services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: pdf-vectordb-qdrant
    ports:
      - "6333:6333" # REST API + Web UI
      - "6334:6334" # gRPC
    volumes:
      - ./qdrant-storage:/qdrant/storage:z
    restart: unless-stopped
